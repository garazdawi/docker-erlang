[%% Kernel/logger
 {kernel, [{logger,[{handler,default,logger_std_h,#{}}]},
           {logger_level,info},
           {inet_dist_listen_min, ${PORT}},
           {inet_dist_listen_max, ${PORT}}
          ]
 },
 {backend, [{service,"${SVC}"}]}
].
