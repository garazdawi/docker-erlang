%% Feel free to use, reuse and abuse the code in this file.

{application, dw_db, [
	{description, "Dockerwatch mnesia backend node."},
	{vsn, "1.0.0"},
	{modules, []},
	{registered, [dockerwatch_sup]},
	{applications, [kernel,
                        stdlib,
                        mnesia
	]},
	{mod, {dw_db_app, []}},
	{env, []}
]}.
