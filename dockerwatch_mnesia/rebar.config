
{deps, [cowboy,
        prometheus,
        prometheus_process_collector,
        prometheus_cowboy]}.

{relx, [{release, {"dockerwatch_mnesia", "1.0.0"}, [dockerwatch_mnesia]},
        {vm_args, "config/vm.args"},
        {sys_config, "config/sys.config"},
        {dev_mode, true},
        {include_erts, false},
        {extended_start_script, true}
    ]}.

{profiles, [{prod, [{relx, [{dev_mode, false},
                            {include_erts, true},
                            {include_src, false}]}]}
           ]}.
%% vim: ft=erlang
